import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
  safeContainer: {
    flex: 1,
    backgroundColor: "Black",
  },
  flatView: {
    top: 20,
    backgroundColor: "red",
    height: 500,
  },
});
